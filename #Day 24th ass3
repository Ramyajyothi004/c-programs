#include <stdio.h>
int main() {
    int s,e,n,num,count,sum,i,arm=0;
    scanf("%d %d", &s,&e);
    num=i;
    sum=i;
    for(i=s;i<=e;i++)
    {
    num=i;
    sum=i;
    while(num!=0)
    {
    n=num%10;
    arm=arm+(n*n*n);
    num=num/10;
    }
    if(sum==arm){
    printf("%d\n",i);
    count++;
    }
    arm=0;
    }
    printf("count=%d",count);

    return 0;
}


OUTPUT
1 1000
1
153
370
371
407
count=5
  
    


    
